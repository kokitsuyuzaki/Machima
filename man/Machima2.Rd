% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Machima2.R
\name{Machima2}
\alias{Machima2}
\title{Symmetric Cross-Omics Cell-Type Deconvolution by Joint Non-Negative Matrix Trifactorization}
\usage{
Machima2(
  X_RNA,
  X_Epi,
  label = NULL,
  T = NULL,
  fixW_RNA = FALSE,
  fixH_RNA = FALSE,
  fixT = TRUE,
  fixH_Sym = FALSE,
  orthW_RNA = FALSE,
  lambda_orthW = 0,
  orthH_RNA = FALSE,
  orthT = FALSE,
  orthH_Sym = FALSE,
  pseudocount = .Machine$double.eps,
  L1_W_RNA = 1e-10,
  L2_W_RNA = 1e-10,
  L1_H_RNA = 1e-10,
  L2_H_RNA = 1e-10,
  L1_T = 1e-10,
  L2_T = 1e-10,
  L1_H_Sym = 1e-10,
  L2_H_Sym = 1e-10,
  orderReg = FALSE,
  horizontal = FALSE,
  J = 3,
  Beta = 2,
  root = FALSE,
  thr = 1e-10,
  viz = FALSE,
  figdir = NULL,
  init = c("Random", "RandomEpi", "RandomRNA"),
  num.iter = 30,
  verbose = FALSE,
  init_W_RNA = NULL,
  init_H_RNA = NULL,
  init_H_Sym = NULL,
  nmf_init_n_restart = 1L,
  nmf_init_num_iter = 30L,
  nmf_init_algorithm = "Frobenius",
  lambda_balance = 0.5,
  T_regularization = c("none", "frobenius_unit", "l2", "low_rank"),
  lambda_T = 0,
  T_rank = NULL,
  H_Sym_structure = c("symmetric", "diagonal"),
  J_hic_only = 0L,
  W_hic_init = NULL,
  fixW_hic = FALSE
)
}
\arguments{
\item{X_RNA}{Single-cell RNA-Seq matrix (n x m) or list of matrices}

\item{X_Epi}{Symmetric epigenome matrix (l x l) or list of symmetric matrices}

\item{label}{A length-m character vector to specify the cell type within X_RNA (Default: NULL)}

\item{T}{Coefficient matrix to connect the dimension of X_RNA and X_Epi (l x n, Default: NULL)}

\item{fixW_RNA}{Fix value option of W_RNA (Default: FALSE)}

\item{fixH_RNA}{Fix value option of H_RNA (Default: FALSE)}

\item{fixT}{If TRUE (default), T is fixed during iteration; when T is also NULL, an identity matrix is auto-constructed per chrom. If FALSE, T is learned as a free dense matrix. For paired scATAC + Hi-C on a shared bin grid, fixT=TRUE is recommended. (Default: TRUE)}

\item{fixH_Sym}{Fix value option of H_Sym (Default: FALSE)}

\item{orthW_RNA}{Deprecated. Use lambda_orthW instead. If TRUE, sets lambda_orthW=1. (Default: FALSE)}

\item{lambda_orthW}{Strength of W_RNA column orthogonality: 0=standard NMF, 1=full orthogonal, intermediate=blend. (Default: 0)}

\item{orthH_RNA}{Orthogonal option of H_RNA (Default: FALSE)}

\item{orthT}{Orthogonal option of T (Default: FALSE)}

\item{orthH_Sym}{Orthogonal option of H_Sym (Default: FALSE)}

\item{pseudocount}{Pseudo count to avoid zero division (Default: Machine Epsilon)}

\item{L1_W_RNA}{Parameter for L1-norm regularization of W_RNA (Default: 1e-10)}

\item{L2_W_RNA}{Parameter for L2-norm regularization of W_RNA (Default: 1e-10)}

\item{L1_H_RNA}{Parameter for L1-norm regularization of H_RNA (Default: 1e-10)}

\item{L2_H_RNA}{Parameter for L2-norm regularization of H_RNA (Default: 1e-10)}

\item{L1_T}{Parameter for L1-norm regularization of T (Default: 1e-10)}

\item{L2_T}{Parameter for L2-norm regularization of T (Default: 1e-10)}

\item{L1_H_Sym}{Parameter for L1-norm regularization of H_Sym (Default: 1e-10)}

\item{L2_H_Sym}{Parameter for L2-norm regularization of H_Sym (Default: 1e-10)}

\item{orderReg}{Order regularization (Default: FALSE)}

\item{horizontal}{Horizontal mode for joint NMF (Default: FALSE)}

\item{J}{Rank parameter to decompose (Default: 3)}

\item{Beta}{Parameter of Beta-divergence (Default: 2)}

\item{root}{Option to add sqrt to the update equation (Default: FALSE)}

\item{thr}{The threshold to stop the iteration (Default: 1e-10)}

\item{viz}{Whether the temporal result is visualized (Default: FALSE)}

\item{figdir}{The figure directory (Default: NULL)}

\item{init}{Initial value algorithm (Default: "Random")}

\item{num.iter}{The number of iteration (Default: 30)}

\item{verbose}{Verbose option (Default: FALSE)}

\item{init_W_RNA}{User-supplied initial W_RNA: a matrix (n x J) or list of matrices (Default: NULL)}

\item{init_H_RNA}{User-supplied initial H_RNA: a (J x m) matrix (Default: NULL)}

\item{init_H_Sym}{User-supplied initial H_Sym: a (J x J) symmetric matrix (Default: NULL)}

\item{nmf_init_n_restart}{Number of NMF restarts for init (Default: 1)}

\item{nmf_init_num_iter}{Number of NMF iterations for init (Default: 30)}

\item{nmf_init_algorithm}{NMF algorithm for init (Default: "Frobenius")}

\item{lambda_balance}{Balance between X_RNA and X_Epi loss terms: 0=RNA only, 1=Epi only, 0.5=equal (default; matches pre-1.3.0). (Default: 0.5)}

\item{T_regularization}{Regularization strategy for T: "none", "frobenius_unit", "l2", or "low_rank". Ignored when fixT=TRUE. (Default: "none")}

\item{lambda_T}{L2 penalty strength for T when T_regularization="l2" (Default: 0)}

\item{T_rank}{Rank of low-rank T parametrization (T=U*t(V)). Required when T_regularization="low_rank". (Default: NULL)}

\item{H_Sym_structure}{Constrain H_Sym to "symmetric" (default; full J(J+1)/2 parameters; symmetric Tucker) or "diagonal" (J diagonal entries only; symmetric CP / sym-PARAFAC). The diagonal option is recommended for cell-type deconvolution of bulk Hi-C. (Default: "symmetric")}

\item{J_hic_only}{Number of Hi-C-only basis columns. When >0, Hi-C reconstruction becomes G_full*H_full*G_full^T with hic-only columns independent of W_RNA. (Default: 0)}

\item{W_hic_init}{Optional list of initial W_hic matrices (each l_k x J_hic_only). (Default: NULL)}

\item{fixW_hic}{If TRUE, do not update W_hic. (Default: FALSE)}
}
\value{
A list containing W_RNA, H_RNA, H_Sym, T, RecError, RelChange. When T_regularization="low_rank", also T_factors. When J_hic_only>0, also W_hic and h_hic.
}
\description{
For symmetric epigenomic matrices (e.g., similarity/correlation matrices).
X_RNA (n x m) is decomposed as W_RNA (n x J) * H_RNA (J x m).
X_Epi (l x l, symmetric) is decomposed as G * H_Sym * t(G),
where G = T * W_RNA (l x J) and H_Sym is J x J symmetric non-negative.
}
\details{
When \code{label} is supplied, dimnames of \code{H_Sym} and rownames of
\code{H_RNA} are assigned by correlating each component's H_RNA row with
per-cluster average expression, then solving a max-weight bipartite matching
(via \code{igraph::max_bipartite_match}). Components that remain unmatched
(e.g. when J > number of clusters) are labeled \code{"comp_j"} instead of NA.
Such components often act as background-absorbing factors that capture the
bulk variance of X_Epi; their H_Sym diagonal entry is typically the largest.
Downstream analyses may drop or baseline-correct \code{"comp_*"} entries.

When \code{H_Sym_structure = "diagonal"}, the model becomes symmetric CP:
\code{X_Epi[k] = sum_i h_i * g_i[k] * g_i[k]^T}, where each component has
an independent contact pattern weighted by \code{h_i = H_Sym[i,i]}. This is
the natural choice when cross-type "interaction" (off-diagonal) is not
biologically meaningful. The default \code{"symmetric"} mode allows off-diagonal
entries to capture co-occurrence of contacts between components.

For paired scATAC + Hi-C on the same genomic bin grid, T is naturally
the identity matrix (the two modalities share the same coordinate axis).
Learning T as a free matrix introduces a fictitious coordinate
transformation that degrades both reconstruction quality and cell-type
discrimination. The v1.2.0 default is therefore \code{fixT = TRUE}.
}
\examples{
X_RNA <- matrix(runif(20*30), nrow=20, ncol=30)
S <- matrix(runif(15*15), nrow=15, ncol=15)
X_Epi <- (S + t(S)) / 2
out <- Machima2(X_RNA, X_Epi, T=NULL, verbose=TRUE)
\dontrun{
# Two-stage workflow: pre-compute NMF, then freeze RNA factors
nmf_res <- nnTensor::NMF(X_RNA, J=3, num.iter=200, algorithm="KL")
out2 <- Machima2(X_RNA, X_Epi,
    init_W_RNA=nmf_res$U, init_H_RNA=t(nmf_res$V),
    fixW_RNA=TRUE, fixH_RNA=TRUE, J=3)
}
}
